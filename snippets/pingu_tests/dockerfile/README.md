# Dockerfile

## Projetos

- `container-project/`: app Node com Dockerfile e docker-compose para smoke test.

Comandos:
- `docker build -t pingu-docker .`
- `docker-compose -f docker-compose.yml up --build`
