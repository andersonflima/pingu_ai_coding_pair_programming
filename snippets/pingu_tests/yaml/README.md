# YAML

## Projetos

- `contracts-project/`: contratos de automação e orquestração com contratos YAML.

Comandos:
- `python -m json.tool < app-config.yaml`
- `docker-compose -f stack-compose.yaml config`
