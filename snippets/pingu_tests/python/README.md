# Python

## Projetos

- `project-fastapi-swagger/`: API REST com FastAPI e documentação automática

Comandos:
- `python -m venv .venv`
- `source .venv/bin/activate`
- `pip install -r requirements.txt`
- `uvicorn app.main:app --reload`
- `pytest`
